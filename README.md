## Getting Start

### Before install

- Config gcloud
- Config goapp
- Config node.js with npm

### Install foreman and start

```
npm install -g foreman
cp .env.sample .env
nf start
```

access to
http://localhost:5000
